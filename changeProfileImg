# Change Profile Image Test (Related to U003 R002)
<pre>
Acceptance Criteria: After the user clicks on the change button and selects image from gallery, the newly selected image will load as their profile image.

1) Click the change button
2) Select gallery
3) Select an image from gallery.
4) Wait until new image uploads to profile picture
</pre>
